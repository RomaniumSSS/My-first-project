# Инструкции для AI-агентов

> **КРИТИЧЕСКАЯ ИНСТРУКЦИЯ:** этот файл — ваша операционная система. Следуйте описанным процессам, ограничениям и стандартам документации.

---

## 1. Инициализация и загрузка контекста
Перед любым действием необходимо восстановить полный контекст.

### Последовательность онбординга
1. **Прочитать `README.md`** — понять цель проекта, стек и статус.
2. **Прочитать всю папку `docs/`** — актуальные архитектурные решения и бизнес-логика. Если папка пуста, зафиксировать этот факт, но сначала убедиться.
3. **Подтвердить готовность** — после чтения перечисленных файлов можно считать кодовую базу понятой.

---

## 2. Протокол поддержки документации
Агент отвечает не только за код, но и за состояние базы знаний.

### Правило «значительного изменения»
Изменения считаются значительными, если затрагивают:
- глобальные пользовательские пути (аутентификация, checkout, критические формы);
- схему БД или API-контракты;
- новые архитектурные компоненты (сервисы, сторы, глобальные провайдеры);
- рефакторинг, меняющий то, *как* работает функция, а не только её расположение.

Если изменение значительное:
1. Немедленно обновите соответствующий файл в `docs/`.
2. При отсутствии подходящего файла создайте новый Markdown в `docs/` и опишите нововведение.

> **Эмпирика:** если новый разработчик не поймёт вашу доработку, прочитав только код, — документация неполна.

---

## 3. Промптинг и когнитивная архитектура
Работаем как старший инженер (GPT-5.1/Claude 4.5 mindset).

### A. Цепочка мыслей (Chain of Thought)
Используйте внутренний план перед кодом:
1. **Анализ:** выявить истинное намерение запроса.
2. **Поиск:** определить файлы/модули, где живёт логика (без угадываний путей).
3. **План:** предложить последовательность действий.
4. **Выполнение:** только после этого пишем код.

### Базовые принципы
- **Documentation-First:** каждая сессия начинается с чтения `docs/` (product.md, tech.md и др.).
- **Instruction-First:** простые задачи — выполняем сразу, сложные — планируем.
- **Keep It Simple (MVP):** избегаем оверинжиниринга; используем стек aiogram + Tortoise ORM + SQLite/PostgreSQL.

---

## 4. Режим планирования (Plan Mode)
Включается, если задача:
- затрагивает несколько модулей (handlers + database + services),
- добавляет внешнюю интеграцию,
- меняет схему БД/миграции,
- содержит архитектурные перестройки.

Если сомневаешься — считаем задачу сложной. Алгоритм:
1. Создай файл `plans/NNN-objective.md` (инкрементальный номер, напр. `001-initial-setup.md`).
2. Включи разделы: **Objective** (цель дословно из запроса), **Proposed Steps**, **Risks**, **Rollback Strategy**.
3. Жди утверждения человека (например, «Ок», «Принято», 👍) перед реализацией.

---

## 5. Система памяти AICODE (Context Anchors)
Оставляй заметки прямо в коде, чтобы будущие агенты понимали решения.

### Общие правила
- Используй формат `# AICODE-<ТИП>: сообщение`.
- Обязателен при неочевидных решениях, долгах или вопросах.

### Типы анкоров
- **AICODE-NOTE:** фиксирует архитектурный выбор или временный хак.  
  Пример: «Используем polling вместо webhook — на dev нет белого IP».
- **AICODE-TODO:** описывает технический долг/недостающий функционал.  
  Вопрос «что сломается на 10 000 пользователей?» помогает понять, нужен ли TODO.
- **AICODE-QUESTION:** открытый вопрос или блокер, который надо эскалировать.

Пример:
```
# AICODE-NOTE: Используем глобальный кэш ради MVP, заменить на Redis.
user_cache = {}

async def get_user(user_id):
    # AICODE-TODO: Добавить обработку ошибок сети
    ...
```

---

## 6. Технические правила (Vibe-Coding Style)
- **Стек:** Python 3.11+, aiogram 3.x, Tortoise ORM, SQLite/PostgreSQL.
- **Асинхронность:** весь I/O (БД, Telegram, HTTP) выполняется через async/await.
- **Структура:** соблюдаем директории из `docs/tech.md`. Не создаём новые файлы в корне, если им место в `handlers/`, `services/` и т.д.
- **Безопасность:** токены и секреты только в `.env`; хардкод запрещён.

---

## 7. Самопроверка перед завершением
Перед тем как сказать «готово», убедись, что:
- задача соответствует требованиям пользователя;
- при наличии плана выполнены все согласованные шаги;
- AICODE-комментарии оставлены там, где решение спорное или неочевидное;
- документация в `docs/` обновлена, если логика изменилась;
- код аккуратен и соответствует PEP8; линтеры не требуются, но формат должен быть чистым.

 Код соответствует задаче?
 Если был план — выполнены ли запланированные на этот этап шаги?
 AICODE: Оставлены ли комментарии (NOTE для хаков, TODO для недоделок) в местах, где код несовершенен?
 Документация (docs/) обновлена, если что-то изменилось?
 Линтеры не нужны, но код должен выглядеть опрятно (PEP8).
 ---

*Этот файл — источник истины для поведения агента в репозитории.*
